# Claude-Build-Test-Loop
Claude Code skill that spawns two agents in a feedback loop — one builds to your spec, one tests against it. They iterate until the result matches. Works for UI, APIs, CLI tools, AI agents, and more.
